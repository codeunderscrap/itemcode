# Item Code Studio - extra dependencies beyond the Python standard library.
#
# Until 7 August 2026 this project deliberately ran with no pip-installable
# OCR/PDF stack (Tesseract-OCR was an external, optional binary). That
# constraint changed: target desktop machines now have internet and admin
# rights, so the PDF/OCR stack is a real pip install instead. See
# agents/CONTRACTS.md's house rule 1 changelog note for the full reasoning.
#
#   pip install -r requirements.txt
#
rapidfuzz
openpyxl
requests

# PDF text/table extraction (digital PDFs)
pymupdf
